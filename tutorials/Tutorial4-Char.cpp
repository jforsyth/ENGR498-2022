#include <iostream>
using namespace std;

int main()
{
	/**
	Use a char* to print out each element of the array 'sentence'. Print each char in
	order and then complete the program with a newline.

	Hint: try the strlen() function to get the length of the array.
	Hint #2: look up what happens if you increment a ptr (e.g. ptr++)
	*/

	char sentence[] = "C++ is better than Python.";

	//Your code here

	return 0;
}